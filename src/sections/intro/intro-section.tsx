import styled from "styled-components";

const SectionContainer = styled.div`
  width: 100%;
  height: 100vh;

`;

const IntroSection = () => {
  return (
    <SectionContainer>
      intro
    </SectionContainer>
  );
};

export default IntroSection;
